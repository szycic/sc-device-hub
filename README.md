# sc-device-hub

## Run

Create or activate the virtual environment, then start the API with:

```bash
PYTHONPATH=src .venv/bin/uvicorn sc_device_hub.web:app --reload
```

Open `http://127.0.0.1:8000/` for the dashboard.